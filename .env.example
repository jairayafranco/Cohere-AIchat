# Variables de entorno para Cohere-AIchat

# API Key de Cohere (REQUERIDA)
# Obtén tu API key en: https://dashboard.cohere.com/api-keys
COHERE_API_KEY=tu_api_key_aqui

# Configuración del modelo (OPCIONAL)
# Modelo de Cohere a utilizar
# Opciones: command, command-light, command-nightly
COHERE_MODEL=command-nightly

# Configuración de generación (OPCIONAL)
# Máximo de tokens a generar por respuesta
MAX_TOKENS=1000

# Temperatura de generación (0.0 - 1.0)
# Valores más bajos = más determinista
# Valores más altos = más creativo
TEMPERATURE=0.7

# Longitud máxima del prompt del usuario (en caracteres)
MAX_PROMPT_LENGTH=2000
